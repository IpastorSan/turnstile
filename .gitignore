# Secrets — never tracked
.env
.env.local

# NOTE: seller/secrets/*.enc IS tracked on purpose. It is Ledger-sealed
# ciphertext; the whole point of the cold-key tier is that it is safe in the
# repo. Do not add *.enc here.

# Dependencies / build output
node_modules/
out/
target/
dist/
.next/

# Local agent scratch
.ralph-loop.sh
.turnstile-eth-secret
.env.bak*
.env.*.bak
