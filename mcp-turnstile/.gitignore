# Generated by scripts/prepack.mjs on `npm pack` / `npm publish`. It is compiled
# output plus copies of files from elsewhere in the repository — everything in it
# is derived, and committing it would mean two copies of the same source drifting.
_bundle/
*.tgz
