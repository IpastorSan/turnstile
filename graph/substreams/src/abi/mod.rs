//! Event decoders generated at build time by `build.rs` from
//! `abi/identity_registry.json`. Not committed and not linted — regenerate with
//! `cargo build`.
#![allow(clippy::all)]

pub mod identity_registry;
